import { render } from '@testing-library/react';
import { GifApp } from '../src/GifApp';

describe('Tests in <GifApp />', () => { 
    
    test('should first', () => { 
        render(<GifApp />);
    });
});